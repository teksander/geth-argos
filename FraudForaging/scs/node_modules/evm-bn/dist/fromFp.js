"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fromFp = void 0;
const bignumber_1 = require("@ethersproject/bignumber");
/**
 * Convert a big number with a custom number of decimals to a stringified fixed-point number.
 */
function fromFp(x, decimals = 18) {
    if (x === undefined || x instanceof bignumber_1.BigNumber === false) {
        throw new Error("Input must be an ethers BigNumber");
    }
    if (decimals < 1 || decimals > 77) {
        throw new Error("Decimals must be between 1 and 18");
    }
    const result = bignumber_1.FixedNumber.fromValue(x, decimals, `fixed256x${decimals}`).toString();
    return result.replace(/.0$/, "");
}
exports.fromFp = fromFp;
